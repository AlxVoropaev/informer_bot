TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_BOT_TOKEN=
ANTHROPIC_API_KEY=
# OPENAI_API_KEY is optional. Without it, deduplication is disabled and the
# owner is DM'd once at startup; otherwise it powers the dedup embeddings.
OPENAI_API_KEY=
OWNER_ID=
LOG_LEVEL=INFO
# Optional dedup tuning:
# DEDUP_THRESHOLD=0.85
# DEDUP_WINDOW_HOURS=48
